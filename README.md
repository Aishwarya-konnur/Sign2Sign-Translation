# Sign2Sign-Translation
The Sign-to-Sign Language Translation System translates one sign language to another, like ASL to ISL. It uses a camera to capture hand gestures, processes them, and uses MediaPipe Hands with models like CNN,RNN,RESNET,MOBILENET,TRANSFORMERS, LSTM, and GRU to recognize signs. The output is shown as text, image, or speech.
